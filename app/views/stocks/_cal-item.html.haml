.primal
  .primal__top
    出荷・入荷

  .items-table
    .items-table__head
      商品一覧
    %table
      %tbody
        %tr
          %th.name
            商品名
          %th.stock
            在庫数
          %th.inc-dec
            在庫の増減

      - @stocks.each do |stock|
        %tr
          %td
            =stock.name
          %td
            =stock.count
          %td
            =form_with model: @stock,method: :patch do |f|
              =f.number_field :count , placeholder: "増減数を入力" 

    .items-table__update
      =link_to "更新",stock_path(@stocks.ids), method: :patch, data: { confirm: '在庫数を更新しますか？' }

    .back
      = link_to "戻る", root_path,class: "back__btn"